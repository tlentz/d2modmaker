package assets

// paths for stuff in the assets/ directory
const (
	AssetDir        = "assets/"
	DataDir         = "113c-data/"
	ModSupportDir   = "modsupport/" // this one is AssetDir + ModSupportDir + cfg.ModName
	GlobalExcelDir  = "global/excel/"
	PatchStringDest = "data/local/LNG/ENG/" // Relative to Output Dir
	DataGlobalExcel = "data/global/excel/"  // Relative to Output Dir
)
